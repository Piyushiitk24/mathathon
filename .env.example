# Example environment configuration (do not commit real secrets)

# Server
PORT=5000
SESSION_SECRET=change-me
ADMIN_PASSWORD=change-me

# MongoDB (use Atlas for production)
MONGODB_URI=
MONGODB_DB_NAME=QuizOc

# Frontend
FRONTEND_URL=http://localhost:3000
